# 批量图片生成系统

一个支持系统提示词和需求提示词组合的批量图片生成工具，基于DALL-E 3 API。

## 🎯 功能特点

- **系统提示词管理**: 预定义图片风格和质量要求
- **需求提示词管理**: 定义具体的图片内容需求
- **批量生成**: 自动组合系统提示词和需求提示词生成图片
- **自定义组合**: 支持指定特定的系统提示词和需求提示词组合
- **历史记录**: 保存生成历史，方便查看和管理
- **交互式界面**: 提供友好的命令行交互界面
- **快速开始**: 预设常用主题，一键生成

## 📁 文件结构

```
├── batch_image_generator.py    # 主要批量生成器
├── image_generator.py          # 基础图片生成器
├── quick_start.py             # 快速开始脚本
├── batch_config.json          # 配置文件 (自动生成)
└── README_批量图片生成.md      # 说明文档
```

## 🚀 快速开始

### 方法1: 使用快速开始脚本

```python
python quick_start.py
```

### 方法2: 直接使用批量生成器

```python
python batch_image_generator.py
```

### 方法3: 编程方式使用

```python
from batch_image_generator import BatchImageGenerator

# 创建生成器
generator = BatchImageGenerator()

# 添加系统提示词
generator.add_system_prompt("education", "教育PPT风格，色彩明亮简洁")

# 添加需求提示词
generator.add_requirement_prompt("AI机器人教学场景")

# 批量生成
results = generator.generate_batch()
```

## 💡 使用示例

### 1. 教育主题图片生成

```python
from quick_start import preset_education_batch

# 生成教育主题图片
results = preset_education_batch()
print(f"成功生成 {results['successful']} 张图片")
```

### 2. 运动AI主题图片生成

```python
from quick_start import preset_sports_ai_batch

# 生成运动AI主题图片
results = preset_sports_ai_batch()
print(f"成功生成 {results['successful']} 张图片")
```

### 3. 自定义批量生成

```python
from quick_start import quick_generate

# 自定义系统提示词
system_prompts = {
    "business": "商务专业风格，适合企业演示",
    "creative": "创意艺术风格，色彩丰富"
}

# 自定义需求提示词
requirement_prompts = [
    "团队协作讨论场景",
    "创新思维头脑风暴"
]

# 批量生成
results = quick_generate(system_prompts, requirement_prompts)
```

## 🔧 高级功能

### 1. 自定义组合生成

```python
from batch_image_generator import BatchImageGenerator

generator = BatchImageGenerator()

# 自定义组合列表
custom_combinations = [
    {"system_key": "education", "requirement_index": 0},
    {"system_key": "business", "requirement_index": 1}
]

# 生成指定组合
results = generator.generate_batch(custom_combinations=custom_combinations)
```

### 2. 指定系统提示词生成

```python
# 只使用特定的系统提示词
results = generator.generate_batch(
    system_key="education",  # 只使用education系统提示词
    requirement_indices=[0, 1, 2]  # 使用前3个需求提示词
)
```

### 3. 查看生成历史

```python
# 显示最近10条生成记录
generator.show_history(limit=10)
```

## 📋 交互式命令行

运行 `batch_image_generator.py` 后可使用以下功能:

1. **查看所有提示词** - 列出当前所有系统提示词和需求提示词
2. **添加系统提示词** - 添加新的系统提示词
3. **添加需求提示词** - 添加新的需求提示词
4. **删除系统提示词** - 删除指定的系统提示词
5. **删除需求提示词** - 删除指定的需求提示词
6. **批量生成图片** - 选择性地生成图片组合
7. **查看生成历史** - 查看最近的生成记录
8. **生成所有组合** - 生成所有可能的提示词组合

## ⚙️ 配置文件

系统会自动创建 `batch_config.json` 配置文件，包含:

- `system_prompts`: 系统提示词字典
- `requirement_prompts`: 需求提示词列表
- `generation_history`: 生成历史记录

## 📁 输出文件

- 图片默认保存在 `batch_images/` 文件夹
- 文件名格式: `{system_key}_{requirement_index}.png`
- 支持自定义保存文件夹

## 🛠️ 依赖要求

- `requests` - HTTP请求库
- `Pillow` - 图片处理库
- 需要配置有效的DALL-E 3 API密钥

## 📝 注意事项

1. **API限制**: 批量生成时会有2秒间隔，避免触发API限制
2. **重试机制**: 内置重试机制，处理网络超时等问题
3. **错误处理**: 完善的错误处理，单个失败不影响整体进程
4. **历史记录**: 自动保存生成历史，方便追踪和管理

## 🎨 预设主题

系统内置以下预设主题:

- **教育主题**: AI教学、数据分析、学习场景
- **运动AI主题**: AI裁判、运动分析、比赛数据
- **商务主题**: 团队协作、项目管理、创新思维

## 📞 使用帮助

如遇到问题:

1. 检查网络连接和API密钥配置
2. 查看 `image_generator.py` 中的API配置
3. 确认安装了所有依赖库
4. 查看生成历史了解具体错误信息

---

**开始使用**: `python quick_start.py` 或 `python batch_image_generator.py`